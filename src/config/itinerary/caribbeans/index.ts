import bahamasItinerary from './bahamasArea/bahamas-itinerary.config';
import bviItinerary from './bviArea/bvi-itinerary.config';
import grenadaItinerary from './grenadaArea/grenada-itinerary.config';
import martiniqueItinerary from './martiniqueArea/martinique-itinerary.config';

// st-martin is intentionally NOT exported — its area stays unwired on
// boat4you (files kept on disk for a possible later launch).
export { bahamasItinerary, bviItinerary, grenadaItinerary, martiniqueItinerary };
